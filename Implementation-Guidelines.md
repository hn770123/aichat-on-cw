# 実装ガイドライン

## 目的

利用者にとって正しく、安全で、保守しやすい実装を目指す。規則を守ること自体よりも、プロダクト全体の品質を優先する。

## 実装方針

- 実装前に、要求の意図、期待する振る舞い、維持すべき既存の振る舞いを確認する。
- 既存コードの設計やデータの流れを理解し、変更全体が一貫する方針を選ぶ。
- 関連する処理は、一連の流れと設計意図を追いやすい形にまとめる。行数や関数の長さだけを理由に分割しない。
- モジュールや関数の境界は、責務の違い、依存関係、再利用性、テストのしやすさなどを踏まえて設ける。小さく分けることを目的にしない。
- 必要なリファクタリングは変更に含めてよい。変更範囲の小ささより、実装全体の整合性と分かりやすさを優先する。
- 外部サービスやライブラリに依存する変更では、最新の公式ドキュメントを確認する。
- 名前、型、コメントには、コードを読むだけでは分かりにくい意図や制約を表す。自明な説明は増やさない。

## 品質確認

- 入力、権限、状態変更、外部入出力、失敗時の挙動を、変更のリスクに応じて検討する。
- テストは利用者から観測できる振る舞いを中心に、変更による不具合を効果的に検出できるものを追加・更新する。
- 変更に適したテスト、型検査、画面確認などを行う。実行できない確認と残るリスクは明記する。
- 最終的に差分だけでなく処理全体を読み、要求の意図に沿った設計になっているかを確認する。

## レビューのための説明

変更説明では、実装を細部ごとに列挙するより、次の点を簡潔に示す。

- 解決する課題と、実装全体の方向性。
- 重要な設計判断と、その理由やトレードオフ。
- 既存の振る舞いへの影響と、主な確認結果。

レビューでは、まず実装の方向性が要求に合っているかを確認し、その後に個々の処理の正しさを確認できる状態を目指す。
