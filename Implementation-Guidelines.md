# 実装ガイドライン

## 1. 目的

この文書は、機能をどのモジュールへ置き、どの粒度で分割し、どこまで検証して変更を完了するかを決めるための実行規則である。目的は、ファイル数や行数を機械的に減らすことではなく、次の状態を継続して保つことにある。

- 変更理由が同じコードは近くにあり、変更理由が異なるコードは境界で分かれている。
- 呼び出し側は、公開契約を読めば利用でき、通常は実装詳細を追わなくてよい。
- 状態変更、外部入出力、失敗時の挙動が追跡できる。
- 変更の影響範囲をテストと型で確認できる。
- 人間にも AI にも、判断の根拠と確認手順を再現できる。

本書では **必須**、**原則**、**推奨**を次の意味で使う。

- **必須**: 例外なく守る。守れない場合は実装を完了扱いにしない。
- **原則**: 通常は守る。外す場合は、変更説明に理由と代替策を書く。
- **推奨**: 費用対効果を確認して採用する。

## 2. 実装を始める前に行うこと

### 2.1 要求を観測可能な完了条件へ変換する

実装前に、要求を次の形式で箇条書きにする。

1. 誰が、どの入口から操作するか。
2. 正常時に何が返り、どの状態が変わるか。
3. 不正入力、権限不足、外部障害時に何が返り、どの状態が残るか。
4. 既存のどの振る舞いを維持するか。
5. 自動テストで何を観測すれば完了と判断できるか。

「読みやすくする」「適切に処理する」のように観測できない表現だけで完了条件を定義してはならない。

### 2.2 既存の変更経路を先に読む

次の順に、変更対象の入口から副作用までを確認する。

1. HTTP、画面操作、ジョブなどの入口。
2. 入力検証と認証・認可。
3. ドメイン上の判断。
4. データベース、AI、静的アセットなどの外部境界。
5. 応答、永続化、ログ。
6. 上記を保証する既存テスト。

類似機能がある場合は、命名、エラー形式、依存の渡し方、テスト方法を合わせる。既存の設計を変更する必要があるなら、機能追加と構造変更を可能な範囲で別のコミットまたは明確に分かれた差分にする。

### 2.3 外部契約を推測しない

外部サービス、ランタイム、ライブラリ、ブラウザー互換性に依存する変更では、**必須**として実装時点の公式ドキュメントを確認する。モデル ID、API の入出力、設定キー、制限値、非推奨機能を記憶だけで決めてはならない。確認した仕様は、テスト、型、設定ファイル、または変更説明のいずれかに検証可能な形で反映する。

## 3. コードを置く場所の決め方

### 3.1 最初に責務を一文で定義する

新しい関数またはモジュールには、「何を受け取り、何を決め、どの副作用を起こすか」を一文で定義する。その一文に無関係な二つ以上の目的が含まれる場合は、責務を分ける。

配置先は次の優先順で決める。

1. 同じ公開契約と同じ変更理由を持つ既存モジュール。
2. その機能だけで閉じる新規モジュール。
3. 複数機能で共有する境界モジュール。

「将来使うかもしれない」だけを理由に共通モジュールを作ってはならない。二つ目の実利用が現れた時点で、同じ意味と契約を持つか確認してから共通化する。

### 3.2 同じモジュールに置く条件

次のいずれかに該当するコードは、**原則**として同じモジュール内で近接させる。

- 同じ状態を読み書きし、一連の不変条件を共同で維持する。
- 片方だけを変更すると、もう片方もほぼ必ず変更する。
- 分離すると、呼び出し順序や事前条件が引数・型に表れず暗黙になる。
- エラー処理を含む一つの短い処理経路として読む必要がある。
- そのモジュール以外から再利用させるべきでない実装詳細である。

ただし、同じファイルに置くことを、巨大な関数を作る理由にしてはならない。処理の段階には名前を付け、局所関数へ分ける。局所関数は必要になるまで公開しない。

### 3.3 別モジュールに分ける条件

次のいずれかに該当し、境界を明示できるコードは分離する。

- 認証、HTTP 応答、会話管理など、変更理由と利用者が明確に異なる。
- 外部サービスや永続化を包み、障害・再試行・形式変換を境界内へ閉じ込められる。
- 二つ以上の呼び出し元が同じ意味と契約で利用する。
- 単独でテストすべき複雑な規則またはアルゴリズムを持つ。
- 公開 API を小さく保ったまま、内部実装を交換できる。

純粋関数であることだけを理由に別ファイルへ移してはならない。数行の変換処理が一か所でしか使われず、呼び出し側の理解に必要なら近くに置く。反対に、副作用がある処理でも、外部境界を隠蔽できるなら分離対象になる。

### 3.4 分離してよい境界の合格条件

新しいモジュール境界は、次をすべて満たしたときだけ採用する。

- 公開する型、関数、定数が必要最小限である。
- 入力、出力、失敗、起こり得る副作用を名前・型・関数コメントから判断できる。
- 呼び出し側が内部状態、実行順序、特定の実装定数を知る必要がない。
- 循環依存を作らない。
- 分離前よりも、代表的な変更で同時に読むファイルまたは知るべき概念が減る。

単にコードを移動して薄い転送関数を増やすだけ、または一つの処理を意味のない層に分割するだけなら分離しない。

### 3.5 ファイルサイズを判断基準にしない

行数の上限は設けない。長いファイルを見つけた場合は、行数ではなく次の兆候を探す。

- 無関係な複数の公開契約がある。
- 変更理由や依存先が複数方向に分かれている。
- ある機能を理解するために、関係のない領域を読み飛ばす必要がある。
- テスト時に大量の無関係な依存を準備する必要がある。

兆候がなければ、長さだけを理由に分割しない。兆候があれば、責務と依存方向を決めてから分割する。

## 4. 関数とデータの設計

### 4.1 公開契約を小さくする

- **必須**: モジュール外で使わない宣言は export しない。
- **原則**: 引数には必要な値だけを渡し、巨大な環境オブジェクトを便宜的に渡さない。ただし、処理全体が同じ外部境界を扱う場合は、依存を個別に分解しすぎない。
- **原則**: boolean 引数で二つの異なる処理を切り替えるより、意図が分かる関数名または判別可能な型を使う。
- **必須**: `unknown` の外部入力は境界で検証し、検証後の型だけを内部へ渡す。
- **必須**: 型アサーションで外部入力の安全性を代用しない。

### 4.2 状態と副作用を明示する

- 計算と外部入出力を分けられる場合は、計算を副作用のない関数にする。
- データベース更新が複数ある場合は、途中失敗時に許容する状態を先に決め、バッチまたはトランザクション相当の仕組みを使う。
- 外部呼び出しの前後で永続化する内容を明記し、失敗時に再実行してよいかを決める。
- 時刻、乱数、外部バインディングなど、テスト結果を不安定にする依存は境界へ寄せる。
- 隠れた共有可変状態を追加しない。必要なら所有者と更新関数を一つに定める。

### 4.3 名前とコメントで意図を残す

- 名前は実装方法ではなく、ドメイン上の目的または保証を表す。
- 各プログラムモジュールには、責務と境界を説明する日本語のヘッダーコメントを書く。
- 各関数には、目的、主要な引数、戻り値、重要な例外または副作用を説明する日本語のヘッダーコメントを書く。
- 行コメントは「何をしているか」の逐語訳ではなく、「なぜ必要か」「何を壊してはいけないか」を記す。
- コードから明白な説明、過去の経緯だけの説明、実装と一致しない説明は残さない。

## 5. 境界ごとの必須規則

### 5.1 HTTP と入力

- リクエストの Content-Type、構文、型、長さ、許可値を利用前に検証する。
- 認証と認可は、保護対象の処理や静的配信より前に行う。
- エラーのステータス、機械判定用コード、公開メッセージを一貫させる。
- 内部例外、スタック、SQL、Secret、認証値を応答に含めない。
- 状態変更では、アプリケーションの認証方式に応じた CSRF 対策を行う。

### 5.2 データベース

- SQL へ値を文字列連結せず、prepared statement とバインド変数を使う。
- 一覧取得では順序と上限を明示する。同値の並び順も安定させる。
- スキーマ変更は既存マイグレーションの書き換えではなく、新しいマイグレーションで行う。ただし、まだ一度も適用されていない初期構築中のスキーマはこの限りでない。
- 削除、再実行、並行更新、途中失敗後に残る状態をテストする。

### 5.3 外部サービス

- 利用可能なモデルや操作などを許可リストで制限し、クライアント入力をそのまま識別子へ使わない。
- 応答は成功時でもランタイムで検証する。
- タイムアウト、再試行、フォールバックの有無を意図的に決める。コストや重複副作用がある処理を自動再試行しない。
- 障害詳細はサーバー側の調査情報に限定し、利用者には安全な文言と必要に応じて追跡 ID を返す。

### 5.4 フロントエンド

- 対象ブラウザーが対応しない構文や API を導入しない。必要なら実機相当環境で確認する。
- 外部入力や AI 応答を `innerHTML` へ渡さず、テキストとして描画する。
- 通信中の二重操作を防ぎ、成功・空・処理中・失敗の各状態を表示する。
- 失敗時に再試行に必要な利用者入力を失わない。
- キーボード操作、フォーカス、状態通知、十分なタッチ領域を維持する。

## 6. テストの作り方

### 6.1 実装と同時に追加するテスト

振る舞いを追加または変更したときは、**必須**として対応するテストを同じ変更に含める。最低限、次を検討する。

- 代表的な正常系。
- 境界値と不正な型・形式。
- 未認証、権限不足、存在しない対象。
- 外部サービスまたは永続化の失敗。
- 状態変更後の保存内容と並び順。
- 機密情報や内部詳細が公開されないこと。
- 修正した不具合を再現する回帰テスト。

内部関数の呼び出し回数だけでなく、利用者または外部境界から観測できる結果を優先して検証する。実装詳細を過度に固定するモックは避けるが、課金や不安定性を伴う外部サービスはテスト用境界に置き換える。

### 6.2 変更後の確認順序

1. 変更箇所に近いテストを実行する。
2. 型検査を実行する。
3. 全自動テストを実行する。
4. UI を変更した場合は対象画面を実際に操作し、表示を確認する。
5. 差分を読み、要求外の変更、デバッグ出力、Secret、生成物がないことを確認する。

環境制約で確認できない項目は、未確認のまま成功扱いにせず、実行したコマンド、制約、残るリスクを変更説明に記載する。

## 7. AI 実装者向けの追加規則

以下は、人手では費用が高くても AI なら反復可能なため、AI が実装する場合は**必須**とする。

### 7.1 変更前の依存地図を作る

コードを編集する前に、変更対象の各シンボルについて次を検索する。

- 定義箇所。
- すべての参照箇所。
- 関連する型、設定、マイグレーション、静的 UI。
- 直接対応するテストとテストデータ。

検索結果から「変更するファイル」「変更しないが確認するファイル」「影響しない根拠」を整理する。ファイル名から影響範囲を推測して編集を始めてはならない。

### 7.2 配置候補を比較してから選ぶ

新しい処理の配置候補を少なくとも「既存モジュール内」と「新規モジュール」の二つで評価する。各候補について次を数え、少ない方を基本案とする。

1. 代表的な変更で同時に読む必要があるファイル数。
2. 境界を越えて渡す必要がある状態の数。
3. 新たに公開するシンボルの数。
4. 暗黙の呼び出し順序または事前条件の数。
5. テストで差し替える必要がある外部依存の数。

数が同程度なら、既存構造と命名に合う方を選ぶ。数値だけで自動決定せず、セキュリティ境界、循環依存、障害の隔離を優先する。

### 7.3 反証レビューを行う

実装後、作成者ではなく批判的レビュアーの立場で次を一項目ずつ確認する。

- 正常系の仮定を一つずつ壊したとき、安全な失敗になるか。
- 入力の欠落、空、最大長、最大長超過、異なる型を処理できるか。
- 外部応答が欠落、空、異形式、例外の場合を処理できるか。
- 同じ操作の再送、途中失敗、並行実行で不整合が起きないか。
- ログと応答に Secret、認証値、個人情報、内部例外が漏れないか。
- 新しい export、依存、設定、コメントのすべてが必要か。
- 要求されていないリファクタリングが混ざっていないか。

見つけた反例は、修正するか、仕様として許容する根拠を変更説明へ残す。

### 7.4 差分を最小化し、整合性は全体で確認する

AI は関連しそうな箇所を一括で書き換えてはならない。既存の公開契約を保てるなら保ち、整形だけの変更を混ぜない。一方で、変更した契約の参照箇所は全件検索し、型、テスト、文書、設定、UI の不整合を残してはならない。

## 8. 実装完了チェックリスト

次をすべて満たしたときだけ、実装完了とする。

- [ ] 要求が観測可能な完了条件になっている。
- [ ] 入口から副作用までの既存経路と参照箇所を確認した。
- [ ] 外部依存を変更した場合、最新の公式仕様を確認した。
- [ ] 配置が責務、凝集度、依存方向に基づいており、行数だけで分割していない。
- [ ] 新しい境界の入力、出力、失敗、副作用が型とコメントから分かる。
- [ ] 外部入力と外部応答を境界で検証している。
- [ ] 認証値、Secret、内部例外をコード、ログ、応答へ漏らしていない。
- [ ] 正常系、異常系、境界値、回帰をテストした。
- [ ] 型検査と全自動テストが通った、または未実行理由と残るリスクを記録した。
- [ ] UI 変更は実際の表示と主要操作を確認した。
- [ ] 最終差分に要求外の変更、不要な export、デバッグコード、生成物がない。

## 9. 判断例

### 例 A: 入力値から会話タイトルを作る短い処理

会話作成でしか使わず、タイトル更新の規則を理解するために必要なら、会話モジュール内の非公開関数にする。純粋関数であっても専用ファイルは作らない。規則が複雑化し、複数の入口で同じ契約を使うようになった時点で分離を再検討する。

### 例 B: API 共通の JSON エラー応答

複数の API が同じ公開形式とセキュリティヘッダーを必要とするため、HTTP 境界のモジュールへ集約する。各機能モジュールは安全に公開できるエラーを表現し、直列化の詳細を重複させない。

### 例 C: Workers AI の呼び出し

一か所からしか呼ばなくても、応答検証、障害変換、モデル制限などが増え、会話保存とは異なる理由で変更されるなら境界モジュールへ分離する。単に `env.AI.run` を一行転送するだけなら抽象化せず、呼び出し側の近くに置く。

### 例 D: 状態を順に更新する処理

複数の更新が同じ不変条件を守り、順序が重要なら一つのユースケースとして地続きに読める場所へ置く。低水準の SQL は補助関数に分けてもよいが、処理順序と途中失敗時の方針を別ファイル間の暗黙知にしない。
